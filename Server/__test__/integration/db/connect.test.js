const { describe, it } = require("@jest/globals");

describe("Database Connection Test", () => {
  it("should successfully connect to the database", () => {

  })

  it("should return an error due to invalid connection string", () => {
    
  })
})